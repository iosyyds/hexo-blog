# 博客部署与「后台数据库」指南

本项目已用 **Hexo + AnZhiYu 主题** 搭建完成，以下是免费上线到互联网、并配置「后台数据库」（评论系统 + 阅读量统计）的完整步骤。

> 全程 0 元：网站托管用 GitHub Pages（免费），评论后端用 Vercel（免费），数据库用 Neon PostgreSQL（免费）。

## ✅ 当前部署状态（已完成）

- **GitHub 仓库**：https://github.com/iosyyds/hexo-blog
- **博客地址**：https://iosyyds.github.io/hexo-blog/
- **状态**：已上线，首页与文章页均验证可访问
- 说明：因你的 `iosyyds.github.io` 已被工具箱网站占用、`blog` 仓库已被个人网站占用，博客独立部署在新建的 `hexo-blog` 仓库（子目录 `/hexo-blog/`），互不影响。

## 🔄 以后如何更新博客

```bash
# 1. 写新文章（Markdown）
hexo new "文章标题"
# 2. 编辑 source/_posts/ 下的 .md 文件
# 3. 生成并部署
hexo clean && hexo g && hexo d
```

`hexo d` 需要 GitHub 认证（推送到 iosyyds/hexo-blog），两种方式任选：
- **方式一（推荐）**：本机已配置 `gh` CLI 并登录，先运行 `gh auth setup-git`，之后 `hexo d` 即可。
- **方式二**：使用 Personal Access Token（Settings → Developer settings → Tokens，勾选 `repo`），运行 `git config --global credential.helper store`，执行 `hexo d` 时按提示输入用户名和 Token（首次后自动记住）。

---

## 一、免费部署到 GitHub Pages（已完成，此节仅供参考）

### 1. 创建 GitHub 账号与仓库
1. 注册/登录 [GitHub](https://github.com)。
2. 点击右上角 **+ → New repository**，仓库名填 **`你的用户名.github.io`**（必须完全等于你的 GitHub 用户名 + `.github.io`），选择 Public。
3. 创建后，在仓库页面 **Settings → Pages → Build and deployment**，Source 选 **`Deploy from a branch`**，Branch 选 **`main`**。

### 2. 修改博客配置文件
编辑 `blog/_config.yml`，改两处：
- `url: https://你的用户名.github.io`
- `deploy.repo: https://github.com/你的用户名/你的用户名.github.io.git`

### 3. 生成并部署
在 `blog` 目录执行：
```bash
hexo clean      # 清理缓存
hexo generate   # 生成静态文件
hexo deploy     # 推送到 GitHub Pages
```
首次 `hexo deploy` 会要求输入 GitHub 账号密码，建议改用 **Token**：
- GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token
- 勾选 `repo` 权限，复制 token
- 部署时提示输入用户名填你的 GitHub 用户名，密码填这个 Token

部署完成后，访问 `https://你的用户名.github.io` 即可看到你的博客。

---

## 二、配置「后台数据库」（评论 + 阅读量统计）—— Waline 免费方案

Waline 是一个开源评论系统，自带**可视化管理后台**（评论审核、删除、邮件通知、阅读量统计），数据存在**独立数据库**里。免费组合：**Vercel（后端）+ Neon PostgreSQL（数据库）**。

### 1. 注册 Vercel 并创建项目
1. 用 GitHub 账号登录 [Vercel](https://vercel.com)。
2. 点击 **Add New → Project**。
3. Import 仓库时选择 Waline 官方仓库：`https://github.com/walinejs/waline`（或使用 Waline 官方提供的一键部署模板）。
4. 点击 **Deploy**，等待部署完成，你会得到一个类似 `https://waline-xxxx.vercel.app` 的地址。

### 2. 创建免费数据库（Neon）
1. 打开 [Neon](https://neon.tech) 注册（可用 GitHub 登录），免费套餐即够用。
2. 在 Vercel 项目页面顶部点 **Storage → Create Database → Marketplace Database Providers → Neon**，跟随引导创建数据库。
3. 在 Neon 控制台左侧选 **SQL Editor**，粘贴 Waline 官方 SQL 建表脚本（来自 `https://waline.js.org/` 部署文档中的 `waline.pgsql`，或用 Neon 引导自动生成），点击 **Run**。

### 3. 配置环境变量
回到 Vercel 项目 → **Settings → Environment Variables**，添加：
| 变量名 | 值 |
| --- | --- |
| `PG_HOST` | Neon 连接串中的主机名（取 `PGHOST_UNPOOLED`） |
| `PG_DB` | 数据库名（取 `PGDATABASE`，一般是 `neondb`） |
| `PG_USER` | 用户名（取 `POSTGRES_USER`） |
| `PG_PASSWORD` | 密码（取 `PGPASSWORD`） |
| `PG_SSL` | `true` |

保存后，到 **Deployments** 里对最新一次部署点 **Redeploy** 重新部署，让环境变量生效。

### 4. 在博客中启用评论
编辑 `blog/_config.anzhiyu.yml`：
```yaml
comments:
  use: waline        # 取消注释，启用 Waline

waline:
  serverURL: https://你的-waline地址.vercel.app   # 填你第一步部署得到的地址
  pageview: true     # 开启阅读量统计（已默认开启）
```

### 5. 重新部署博客
```bash
hexo clean && hexo generate && hexo deploy
```

### 6. 登录管理后台
浏览器打开 `https://你的-waline地址.vercel.app/ui`，设置管理员密码后即可管理评论、查看数据。

---

## 三、日常使用命令

| 命令 | 作用 |
| --- | --- |
| `hexo new "文章标题"` | 新建一篇文章 |
| `hexo s` | 本地预览（浏览器打开 http://localhost:4000） |
| `hexo clean && hexo g` | 重新生成静态文件 |
| `hexo d` | 一键部署上线 |

写文章：编辑 `blog/source/_posts/` 下的 `.md` 文件，支持 Markdown 语法。

---

## 四、主题个性化（可选）

所有主题配置都在 `blog/_config.anzhiyu.yml`（主题作者的完整文档：https://anzhiyu-docs.vercel.app/ ）：

- **站点头像/名称/简介**：搜索 `avatar`、`site_slogan` 等字段
- **导航菜单**：搜索 `menu` 字段
- **分类/标签页**：创建 `source/categories/index.md`、`source/tags/index.md` 等页面（作者文档有现成模板）
- **友链**：搜索 `link` / `friends` 字段

---

## 五、FAQ

**Q：我不想用评论，只想纯展示？**
A：什么都不用配置，评论系统默认关闭，不影响使用。

**Q：Vercel / Neon 免费额度够吗？**
A：够。Vercel Hobby 免费套餐个人博客足够；Neon 免费版提供约 0.5GB 存储，评论数据量对个人博客而言完全用不完。

**Q：能绑定自己的域名吗？**
A：可以，GitHub Pages 和 Vercel 都支持免费绑定自定义域名（需自行购买域名）。
